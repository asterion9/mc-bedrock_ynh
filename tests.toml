test_format = 1.0

[default]
# Keep CI shallow while app packaging is still in progress:
# only verify basic non-web installation flow.
exclude = [
  "install.root",
  "install.subdir",
  "install.private",
  "change_url",
]
